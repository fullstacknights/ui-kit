enum States {
  default = "default",
  hover = "hover",
  pressed = "pressed",
  disabled = "disabled"
}

enum Sizes {
  xs = "xs",
  sm = "sm",
  rg = "rg",
  md = "md",
  lg = "lg",
  xl = "xl"
}

enum Headers {
  subheader2 = "subheader2",
  subheader = "subheader",
  header = "header"
}

enum Variants {
  primary = "primary",
  secondary = "secondary",
  inverse = "inverse"
}

export { States, Sizes, Headers, Variants };
